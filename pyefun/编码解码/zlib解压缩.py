import zlib
import binascii


def zlib压缩(data):
    """
    zlib压缩 的功能说明（请补充）。

    Args:
        data: 参数说明。

    """
    return zlib.compress(data)

def zlib解压(data):
    """
    zlib解压 的功能说明（请补充）。

    Args:
        data: 参数说明。

    """
    return zlib.decompress(data)
